# Railway deployment configuration - Optimized Structure
[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm install && npx prisma generate && npm run build && npx prisma migrate deploy"

[deploy]
startCommand = "cd backend && node ./dist/main.js"
healthCheckPath = "/"
