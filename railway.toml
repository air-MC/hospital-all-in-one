# Railway Config - Force Rebuild: 2026-01-19 19:59 (Security Update - Block system@hospital.com)
[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm install && npx prisma generate && npm run build"

[deploy]
startCommand = "echo '🚀 Starting Deployment...' && cd backend && npx prisma db push --accept-data-loss && npm run start:prod"
healthCheckPath = "/"
