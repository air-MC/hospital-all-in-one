# Railway deployment configuration - Final Data Population
[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm install && npx prisma generate && npm run build"

[deploy]
startCommand = "echo '🚀 Starting Deployment...' && cd backend && npx prisma db push && npx ts-node prisma/seed_production.ts && node $(find dist -name main.js | head -n 1)"
healthCheckPath = "/"
