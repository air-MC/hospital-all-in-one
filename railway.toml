# Railway Config - Force Rebuild: 2026-01-19 15:28 (Doctor Schedule Schema Update)
[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm install && npx prisma generate && npm run build"

[deploy]
startCommand = "echo '🚀 Starting Deployment...' && cd backend && npx prisma db push --accept-data-loss --force-reset && npx ts-node prisma/seed_production.ts && node $(find dist -name main.js | head -n 1)"
healthCheckPath = "/"
