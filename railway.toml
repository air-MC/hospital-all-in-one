# Railway deployment configuration - Safety Mode
[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm install && npx prisma generate && npm run build"

[deploy]
startCommand = "cd backend && node ./dist/main.js"
healthCheckPath = "/"
